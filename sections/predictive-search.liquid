{%- if predictive_search.performed -%}
  <div id="predictive-search-results">
    <ul
      id="predictive-search-results-list"
      class="predictive-search__results-list"
      role="listbox"
      aria-labelledby="predictive-search-products">
      {%- for product in predictive_search.resources.products -%}
        <li
          class="predictive-search__list-item mb-4"
          role="option"
          {% if product.tags contains 'Breeders only' %}
          {% unless customer.tags contains 'breeder' %}
          style="display: none"
          {% endunless %}
          {% endif %}>
          <a
            href="{{ product.url }}"
            class="predictive-search__item flex justify-between items-center border-b border-gray-300 pb-4 group"
            tabindex="-1">
            <div class="flex items-center gap-4">
            {%- if product.featured_media != blank -%}
              <img
                class="predictive-search__image w-20 h-20 rounded-xl bg-green_custom overflow-hidden"
                src="{{ product.featured_media | img_url: '150x' }}"
                alt="{{ product.featured_media.alt }}"
                loading="lazy"
                width="50"
                height="{{ 50 | divided_by: product.featured_media.preview_image.aspect_ratio }}">
            {%- endif -%}
            <div class="predictive-search__item-content">
              <span class="predictive-search__item-heading group-hover:underline font-norman font-helvetica">{{ product.title }}</span>
              {%- if settings.predictive_search_show_price -%}
                {% comment %}Render price{% endcomment %}
              {%- endif -%}
            </div>
            </div>
            <div class="font-helvetica font-bold">{{ product.price | money }}</div>
          </a>
        </li>
      {%- endfor -%}

<!--

      <li class="predictive-search__list-item" role="option">
        <button class="predictive-search__item" tabindex="-1">
          Search for “{{ predictive_search.terms }}”
        </button>
      </li>
    </ul>
    {%- if predictive_search.resources.collections.size > 0 -%}
      <h3 id="predictive-search-collections" class="predictive-search__heading">
        Collections
      </h3>
    {%- endif -%}
    <ul
      id="predictive-search-results-list"
      class="predictive-search__results-list"
      role="listbox"
      aria-labelledby="predictive-search-collections">
      {%- for collection in predictive_search.resources.collections -%}
        <li class="predictive-search__list-item" role="option">
          <a
            href="{{ collection.url }}"
            class="predictive-search__item collection-search-item"
            tabindex="-1">
            {%- if collection.featured_media != blank -%}
              <img
                class="predictive-search__image"
                src="{{ collection.featured_media | img_url: '150x' }}"
                alt="{{ collection.featured_media.alt }}"
                width="50"
                height="{{ 50 | divided_by: collection.featured_media.preview_image.aspect_ratio }}">
            {%- endif -%}
            <div class="predictive-search__item-content">
              {%- if settings.predictive_search_show_vendor -%}
                <span class="visually-hidden">Vendor</span>
                <div class="predictive-search__item-vendor">{{ collection.vendor }}</div>
              {%- endif -%}
              <span class="predictive-search__item-heading">{{ collection.title | split: ' - Paw Pantry' }}</span>
              {%- if settings.predictive_search_show_price -%}
                {% comment %}Render price{% endcomment %}
              {%- endif -%}
            </div>
          </a>
        </li>
      {%- endfor -%}
      <li class="predictive-search__list-item" role="option">
        <button class="predictive-search__item" tabindex="-1">
          Search for “{{ predictive_search.terms }}”
        </button>
      </li>
    </ul>
  </div>
{%- endif -%}

-->